<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Catálogo Alimentuus 2026</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: white;
            /* Garante que as cores de fundo saiam na impressão */
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        /* --- CABEÇALHO DO PDF --- */
        .header-catalogo {
            background-color: #006837; /* Verde Bandeira */
            color: white;
            padding: 40px 20px; /* Aumentei um pouco o espaçamento */
            text-align: center;
            margin-bottom: 50px;
            border-bottom: 6px solid #8CC63F; /* Faixa Verde Limão */
        }

        /* --- CARDS DOS PRODUTO --- */
        .produto-card {
            border: 1px solid #ddd;
            padding: 20px;
            margin-bottom: 30px;
            page-break-inside: avoid; /* Evita quebrar o card no meio da folha */
            text-align: center;
            background: #fff;
            
            /* -> AQUI ESTÁ O SEGREDO DO TAMANHO FIXO <- */
            height: 520px; /* Altura fixa para todos */
            overflow: hidden; 
            display: flex;
            flex-direction: column;
            /* ----------------------------------------- */
        }

        .produto-img {
            height: 180px;
            width: 100%;
            object-fit: contain;
            margin-bottom: 20px;
            flex-shrink: 0; /* Garante que a imagem não amasse */
        }

        .produto-titulo {
            color: #006837;
            font-weight: 800;
            text-transform: uppercase;
            font-size: 1.2rem;
            margin-bottom: 10px;
            height: 60px; /* Reserva espaço fixo para 2 linhas de título */
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .produto-peso {
            font-size: 1.1rem;
            color: #000;
            font-weight: bold;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #8CC63F;
        }

        .texto-descricao {
            color: #333;
            font-size: 0.95rem;
            line-height: 1.4;
            /* O texto vai ser cortado aqui se for muito longo */
        }

        /* --- REGRAS CRÍTICAS PARA IMPRESSÃO --- */
        @media print {
            /* Oculta TUDO que não seja do catálogo */
            .navbar, header, .btn, .d-print-none, nav {
                display: none !important;
            }
            
            body, html {
                margin: 0;
                padding: 0;
            }
            
            @page { margin: 1.5cm; }
        }
    </style>
</head>

<body>

    <div class="header-catalogo d-flex justify-content-center align-items-center gap-5">
        
        <img src="/uploads/logo-boi.png" alt="Logo" style="height: 140px;>

        <div class="text-start">
            <h1 class="mb-0 fw-bold display-4" style="line-height: 1;">ALIMENTUUS</h1>
            <h3 class="fw-light mb-0">Nutrição Animal</h3>
            <p class="mt-3 mb-0 opacity-75 fs-5">Catálogo de Produtos 2026</p>
        </div>

    </div>

    <div class="container">
        <div class="row">
            {{#each produtos}}
            <div class="col-4 mb-4"> <div class="produto-card shadow-sm">
                    <img src="/uploads/{{this.imagem}}" class="produto-img">
                    
                    <div class="produto-titulo">
                        <span>{{this.nome}}</span>
                    </div>
                    <div class="produto-peso">{{this.peso}}</div>

                    <div class="texto-descricao">
                        {{this.descricao_curta}}
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <div class="d-print-none position-fixed top-0 end-0 p-3">
        <button onclick="window.print()" class="btn btn-success shadow-lg btn-lg d-block w-100 mb-2">
            🖨️ Imprimir PDF
        </button>
        <button onclick="window.close()" class="btn btn-secondary shadow-lg btn-lg d-block w-100">
            Fechar
        </button>
    </div>

</body>
</html>