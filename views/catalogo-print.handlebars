<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Catálogo Alimentuus</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* --- ESTILOS DO PDF --- */
        
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: white;
            -webkit-print-color-adjust: exact; /* Força imprimir cores de fundo */
            print-color-adjust: exact;
        }

        /* CABEÇALHO VERDE (Só existe um agora!) */
        .header-catalogo {
            background-color: #006837; /* Verde Bandeira */
            color: white;
            padding: 30px;
            text-align: center;
            margin-bottom: 40px;
            border-bottom: 5px solid #8CC63F; /* Faixa Verde Limão */
        }

        /* CARD DO PRODUTO */
        .produto-card {
            border: 1px solid #ddd;
            padding: 15px;
            margin-bottom: 30px;
            page-break-inside: avoid; /* Evita quebrar ao meio na impressão */
            text-align: center;
        }

        .produto-img {
            height: 180px;
            width: 100%;
            object-fit: contain;
            margin-bottom: 15px;
        }

        .produto-titulo {
            color: #006837; /* Verde Escuro */
            font-weight: 800;
            text-transform: uppercase;
            font-size: 1.1rem;
            margin-bottom: 5px;
        }

        /* LETRAS PRETAS (Contraste Máximo para Daltonismo) */
        .produto-peso {
            font-size: 1rem;
            color: #000000; /* Preto Total */
            font-weight: bold;
            margin-bottom: 10px;
        }

        .texto-descricao {
            color: #000000; /* Preto Total */
            font-size: 0.9rem;
        }

        /* --- O PULO DO GATO PARA IMPRESSÃO --- */
        @media print {
            /* Esconde menus, botões e barras do site */
            .navbar, header, .btn, .d-print-none {
                display: none !important;
            }
            
            /* Ajusta margens do papel */
            @page { margin: 1cm; }
        }
    </style>
</head>

<body>

    <div class="header-catalogo">
        <img src="/uploads/logo-boi.png" alt="Logo" style="height: 80px; margin-bottom: 15px; filter: brightness(0) invert(1);">
        <h1 class="mb-0 fw-bold">ALIMENTUUS</h1>
        <h4 class="fw-light">Nutrição Animal de Alta Performance</h4>
        <p class="mt-2 mb-0 small">Catálogo de Produtos 2026</p>
    </div>

    <div class="container">
        <div class="row">
            {{#each produtos}}
            <div class="col-4">
                <div class="produto-card">
                    <img src="/uploads/{{this.imagem}}" class="produto-img">
                    
                    <h4 class="produto-titulo">{{this.nome}}</h4>
                    <div class="produto-peso">Embalagem: {{this.peso}}</div>

                    <div class="mt-3 texto-descricao">
                        {{this.descricao_curta}}
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <div class="d-print-none position-fixed top-0 end-0 p-3">
        <button onclick="window.print()" class="btn btn-primary shadow-lg btn-lg mb-2 d-block w-100">
            🖨️ Imprimir
        </button>
        <button onclick="window.close()" class="btn btn-secondary shadow-lg btn-lg d-block w-100">
            Fechar
        </button>
    </div>

</body>
</html>