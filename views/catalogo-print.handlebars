<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="UTF-8">
        <title>Catálogo Alimentuus 2026</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        
        <style>
            /* ESTILO DE REVISTA / PDF */
            body {
                background-color: #fff;
                font-family: 'Helvetica Neue', Arial, sans-serif;
                -webkit-print-color-adjust: exact;
            }

            .header-catalogo {
                background-color: #198754; 
                color: white;
                padding: 40px;
                text-align: center;
                margin-bottom: 40px;
            }

            .produto-card {
                border: 1px solid #eee;
                padding: 15px;
                margin-bottom: 30px;
                page-break-inside: avoid; 
            }

            .produto-img {
                height: 200px;
                width: 100%;
                object-fit: contain; /* Ajusta a imagem sem esticar */
                margin-bottom: 15px;
            }

            .produto-titulo {
                color: #198754;
                font-weight: 800;
                text-transform: uppercase;
                font-size: 1.1rem;
                margin-bottom: 5px;
                border-bottom: 2px solid #198754;
                display: inline-block;
            }

            .produto-peso {
                font-size: 0.9rem;
                color: #666;
                font-weight: bold;
                margin-bottom: 10px;
            }

            .tabela-tecnica {
                background-color: #f8f9fa;
                padding: 10px;
                border-radius: 5px;
                font-size: 0.75rem;
                font-family: monospace;
                
                column-count: 2; 
                column-rule: 1px solid #ddd;
                white-space: pre-wrap; /* Respeita os Enters */
            }
        </style>
    </head>
    <body>

        <div class="header-catalogo">
            <h1>ALIMENTUUS</h1>
            <h3>Nutrição Animal de Alta Performance</h3>
            <p>Catálogo de Produtos 2026</p>
        </div>

        <div class="container">
            <div class="row">
                
                {{#each produtos}}
                <div class="col-4">
                    <div class="produto-card">
                        <img src="https://cdn-icons-png.flaticon.com/512/1581/1581645.png" class="produto-img" alt="Saco de Ração">
                        
                        <div class="text-center">
                            <h4 class="produto-titulo">{{this.nome}}</h4>
                            <div class="produto-peso">{{this.peso}}</div>
                        </div>

                        <div class="mt-3 text-center text-secondary small">
                            {{this.descricao_curta}}
                        </div>
                    </div>
                </div>
                {{/each}}

            </div>
        </div>

    </body>

    <div class="d-print-none position-fixed top-0 end-0 p-3" style="z-index: 1000;">
        <button onclick="window.print()" class="btn btn-primary shadow-lg btn-lg">
            🖨️ Imprimir / Salvar PDF
        </button>
        <button onclick="window.close()" class="btn btn-secondary shadow-lg btn-lg">
            Fechar
        </button>
    </div>

    <style>
        /* Esconde o botão quando for imprimir */
        @media print {
            .d-print-none { display: none !important; }
        }
    </style>

</html>