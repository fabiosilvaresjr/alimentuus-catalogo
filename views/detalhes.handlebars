<div class="container mt-5">
    <a href="/" class="btn btn-outline-secondary mb-3">← Voltar para o Catálogo</a>

    <div class="row">
        <div class="col-md-5">
            <div class="card border-0 shadow-sm">
                <div style="height: 400px; background-color: #e9ecef; display: flex; align-items: center; justify-content: center;">
                    <span class="text-muted">Foto do Produto</span>
                </div>
            </div>
        </div>

        <div class="col-md-7">
            <span class="badge bg-success mb-2">{{produto.categoria}}</span>
            <h1 class="display-5 fw-bold text-success">{{produto.nome}}</h1>
            
            <h3 class="text-dark mt-3">R$ {{produto.preco}}</h3>
            <p class="text-muted">Embalagem: {{produto.peso}}</p>

            <hr>

        <div class="col-md-7">
            <p class="lead mt-3">{{produto.descricao_curta}}</p>

        <hr>

        <h5 class="text-success">Níveis de Garantia</h5>
            <div class="p-3 bg-light rounded border">
            <p style="white-space: pre-wrap; font-family: monospace;">{{produto.ficha_tecnica}}</p>
        </div>

        </div>

            <div class="mt-4 d-grid gap-2">
                <button class="btn btn-success btn-lg">📞 Solicitar Cotação no WhatsApp</button>
            </div>
        </div>
    </div>
</div>