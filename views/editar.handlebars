<div class="card shadow">
    <div class="card-header bg-warning text-dark">
        <h4 class="mb-0">✏️ Editar Produto</h4>
    </div>
    <div class="card-body">
        <form action="/produtos/atualizar" method="POST">
            
            <input type="hidden" name="id" value="{{produto.id}}">

            <div class="mb-3">
                <label>Nome do Produto:</label>
                <input type="text" name="nome" class="form-control" value="{{produto.nome}}" required>
            </div>

            <div class="row">
                <div class="col-md-4 mb-3">
                    <label>Categoria:</label>
                    <input type="text" name="categoria" class="form-control" value="{{produto.categoria}}">
                </div>
                <div class="col-md-4 mb-3">
                    <label>Peso/Embalagem:</label>
                    <input type="text" name="peso" class="form-control" value="{{produto.peso}}">
                </div>
                <div class="col-md-4 mb-3">
                    <label>Preço (R$):</label>
                    <input type="number" step="0.01" name="preco" class="form-control" value="{{produto.preco}}" required>
                </div>
            </div>

            <div class="mb-3">
                <label>Descrição Curta (Comercial):</label>
                <textarea name="descricao_curta" class="form-control" rows="2">{{produto.descricao_curta}}</textarea>
            </div>

            <div class="mb-3">
                <label>Ficha Técnica (HTML/Texto):</label>
                <textarea name="ficha_tecnica" class="form-control" rows="5">{{produto.ficha_tecnica}}</textarea>
            </div>

            <button type="submit" class="btn btn-success w-100">💾 Salvar Alterações</button>
            <a href="/" class="btn btn-outline-secondary w-100 mt-2">Cancelar</a>
        </form>
    </div>
</div>